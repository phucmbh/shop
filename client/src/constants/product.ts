export enum URL {
  PRODUCT = 'product',
  PRODUCTS = 'products'
}

export enum SORT_BY {
  CREATED_AT = 'createdAt',
  VIEW = 'view',
  SOLd = 'sold',
  PRICE = 'price'
}

export enum ORDER {
  ASC = 'asc',
  DESC = 'desc'
}
