export enum SORT_BY {
  CREATED_AT = 'createdAt',
  VIEW = 'view',
  SOLd = 'sold',
  PRICE = 'price',
  CATEGORY = 'category'
}

/**
 * THREE_MINUTES = 3 * 60 * 1000
 */
export const THREE_MINUTES = 3 * 60 * 1000

export enum ORDER {
  ASC = 'asc',
  DESC = 'desc'
}
